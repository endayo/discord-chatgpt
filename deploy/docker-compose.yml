name: discord-chatgpt-bot

services:
  bot:
    build:
      context: ..                         # ← プロジェクトルートをコンテキストに
      dockerfile: deploy/build/node/Dockerfile
    # 固定名にしたければ有効化
    # container_name: discord-bot
    restart: unless-stopped
    env_file:
      - ./.env  